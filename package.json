{
	"name": "geolocator-node",
	"version": "1.0.0",
	"description": "Get Google Maps coordinates by many address from input file and write latitude and longitude of them in output files",
	"main": "geo.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Henrique da Mota",
	"license": "ISC",
	"dependencies": {
		"@google/maps": "^1.1.3",
		"dotenv": "^10.0.0"
	}
}
